_target_: resnet
layers: 50
pretrained: false
optimizer:
  _target_: optimizer
  type: adam
  learning_rate: "slow"  # Wrong! Should be float, not string
  weight_decay: 0.0001
  betas: [0.9, 0.999]
scheduler:
  _target_: scheduler
  type: cosine
  warmup_epochs: 5
  min_lr: 0.00001
